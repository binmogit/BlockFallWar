// src/blockConfig.ts
// Centralized config for block defaults (fallInterval, moveInterval)

export const BLOCK_DEFAULTS = {
  // Default color for blocks when none is provided. Use a pleasant red tone.
  defaultColor: '#ef4444',
  fallInterval: 250, // ms
  moveInterval: 100, // ms
};
